{% extends "base/base" %}

{% block head %}
    <link rel="stylesheet" href="/static/css/style.css">
{% endblock head %}

{% block body %}
    <div class="wrapper">
        {% include "components/nav" %}
        <div class="container">
            <h1>Home</h1>
            {% if data.path.specific.tag == 'File' %}
                {{ data.path.name }}
                <br>
                <a href="/file/{{ data.path.key }}">download</a>
            {% else %}
                <div>
                    <h2>{{ data.path.path }}</h2>
                    <p>{{ data.path.key }}</p>
                    <ul>
                        {% if data.parent %}
                            <li><a href="/shared/{{ parent.key }}">...</a></li>
                        {% endif %}
                        {% for child in data.children | sort(attribute='specific.tag') %}
                            <li>
                                <a href="/shared/{{ child.key }}">
                                    {{ data.child.name }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}
        </div>
    </div>
{% endblock body %}